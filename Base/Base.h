/* *  Base.h *  CCGame * *  Created by linux_wuliqiang@163.com on 2015-03-30. *  Copyright 2015 Beijing. All rights reserved. * *	游戏全局配置文件 */#ifndef __Base_h__#define __Base_h__// 系统底层需要包含的文件#include "cocos2d.h"#include "cocos-ext.h"#include "cocostudio/CocoStudio.h"#include "ui/CocosGUI.h"using namespace std;using namespace cocos2d;using namespace cocos2d::extension;using namespace cocostudio;// Base 文件夹模块的包含文件#include "BaseConfig.h"#include "BaseTypeDefine.h"#include "Platform_Ios.h"#include "BaseUtil.h"#include "BaseStream.h"#include "BaseMusicList.h"#include "BaseMusic.h"#include "BaseStepPathFinder.h"#include "BaseTiledMap.h"#include "CSVTable.h"#include "CSVParse.h"#include "CSVFileReader.h"#include "BaseScript.h"#include "BaseLayer.h"//#include "Platform_Ios.h"// 游戏框架的初始化和释放工作void BaseInit(void);void BaseFree(void);void BasePause(void);void BaseResume(void);void BaseRun(float _dt);void BasePaint(float _dt);/* * 说明：iphone4 中 通过设置屏幕坐标缩放系数 [[CCDirector sharedDirector] setContentScaleFactor:2.0]; 后， *		默认情况下， 游戏中调用 setPosition 来设置位置的时候， 所有的坐标信息会自动 放大 2倍 *		如果不想用系统的缩放来进行定位， 可以通过 setPositionInPixels 来设置绝对坐标显示  */typedef enum{	DISP_WXVGA =0,			// 标清版本 (320x480)--iphone 3	DISP_RETINA,			// 高清版本 (640x960)--iphone 4    DISP_IP5,               // 高清 iphone5 (640x1136)    DISP_IP6,               // 高清 iphone6 (750x1334)    DISP_IP6_PLUS,          // 高清 iphone5 (1242 × 2208)	DISP_IPAD,				// Ipad版本 (1024x768)		DISP_End        }DisplayMode;extern DisplayMode		gDisplayMode;       // 显示模式#endif // __Base_h__